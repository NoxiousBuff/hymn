import 'package:flutter/material.dart';
import 'dart:math';

Color kRandomColor =
    Colors.primaries[Random().nextInt(Colors.primaries.length)];

TextStyle kScrollPartText = TextStyle(
  fontSize: 24.0,
  fontWeight: FontWeight.w500,
);
